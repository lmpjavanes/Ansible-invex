Host,CheckedAt,HostStatus,Operation,Username,Action,UserType,Group,ExtraGroups,Home,Shell,Changed,Status,ChageInfo,Message
{% for h in (audit_linux | default([])) %}
{% set ops = h.operations | default([]) %}
{% if ops | length == 0 %}
{{ h.host | default('') }},{{ h.checked_at | default('') }},{{ h.status | default('') }},{{ h.operation | default('') }},,,,,,{{ '' }},,{{ '' }},{{ h.status | default('') }},,{{ (h.message | default('')) | replace('\n',' ') | replace('\r',' ') | replace(',',';') }}
{% else %}
{% for op in ops %}
{{ h.host | default('') }},{{ h.checked_at | default('') }},{{ h.status | default('') }},{{ h.operation | default('') }},{{ op.username | default('') }},{{ op.action | default('') }},{{ op.user_type | default('') }},{{ op.group | default('') }},{{ (op.extra_groups | default([])) | join('|') }},{{ op.home | default('') }},{{ op.shell | default('') }},{{ op.changed | default(false) }},{{ op.status | default('') }},{{ (op.chage_info | default('')) | replace(',',';') }},{{ (op.message | default('')) | replace('\n',' ') | replace('\r',' ') | replace(',',';') }}
{% endfor %}
{% endif %}
{% endfor %}