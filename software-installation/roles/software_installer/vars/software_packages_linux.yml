---
# Lista de paquetes a instalar en servidores Linux.
# ============================================
# PAQUETE: [NOMBRE]
# FECHA: [FECHA]
# AGREGADO POR: [PERSONA]
# ============================================
#  - name: "[nombre-del-paquete]"
#    description: "[descripción detallada]"
#    vendor: "[proveedor]"
#    src_file: "[archivo.rpm]"
#    dest_path: "/tmp/"  # o /home/{{ ansible_user }}/
#    package_type: "rpm"
#    dependencies:
#      - "[dependencia1]"
#      - "[dependencia2]"
#    commands:
#      install: "rpm -ivh"
#      post_install_validation: "rpm -qa | grep -i [nombre]"
#      activation: "[comando de activación]"  # Descomentar si aplica
#      start: "[comando para iniciar]"        # Descomentar si aplica
#      status: "[comando para status]"        # Descomentar si aplica
#    version_regex: "[nombre]-(\\d+.*)"

linux_packages:
  # Paquete: htop
  - name: "htop"
    description: "Visor interactivo de procesos para Linux"
    vendor: "htop.dev"
    src_file: "htop-3.3.0-1.el9.x86_64.rpm"
    dest_path: "/tmp/"
    package_type: "rpm"
    # Dependencias del paquete (se instalarán automáticamente)
    dependencies:
      - "hwloc"
      - "ncurses"
    commands:
      install: "rpm -ivh"
      post_install_validation: "rpm -qa | grep -i htop"
    version_regex: "htop-(\\d+\\.\\d+\\.\\d+)"

  # Paquete: SentinelAgent (ejemplo para futuro)
  # - name: "SentinelAgent"
  #   description: "Agente de seguridad SentinelOne"
  #   vendor: "SentinelOne"
  #   src_file: "SentinelAgent_Linux_x86_64_v25_1_3_6.rpm"
  #   dest_path: "/home/{{ ansible_user }}/"
  #   package_type: "rpm"
  #   dependencies:
  #     - "glibc"
  #     - "libstdc++"
  #   commands:
  #     install: "rpm -ivh"
  #     post_install_validation: "rpm -qa | grep -i SentinelAgent"
  #     activation: "/opt/sentinelone/bin/sentinelctl management token set {{ sentinelone_token }}"
  #     start: "/opt/sentinelone/bin/sentinelctl control start"
  #     status: "/opt/sentinelone/bin/sentinelctl control status"
  #   version_regex: "SentinelAgent-(\\d+.*)"